import { EditorProps } from "@tiptap/pm/view";

const Props: EditorProps = {
  attributes: {
    class:
      "prose-base prose-gray prose-headings:font-medium focus:outline-none",
  },
};

export default Props;
